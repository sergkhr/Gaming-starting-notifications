# Gaming starting notifications
analyzes chat for invites to play, then sends notifications

# Installation
Create .env file - check  .env_pattern.txt for that (basically copy paste)

# Test
docker compose run --rm notifier python main.py --test